{
	"name": "forgetrail-site",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "filepress dev",
		"build": "filepress build",
		"preview": "filepress preview",
		"check": "filepress check",
		"ship": "pnpm build && wrangler pages deploy build --project-name=forgetrail",
		"deploy": "pnpm ship"
	},
	"devDependencies": {
		"getfilepress": "^0.1.4",
		"wrangler": "^4.120.1"
	},
	"pnpm": {
		"onlyBuiltDependencies": ["esbuild", "getfilepress", "workerd"]
	}
}
